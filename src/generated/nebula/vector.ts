// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.4.1
//   protoc               v6.33.5
// source: nebula/vector.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Value } from "./common";

/** gRPC requires the proto3 syntax */

export interface Row {
  values: Value[];
}

export interface ValueType {
  valueType: Buffer;
}

export interface RowType {
  numColumns: number;
  columnNames: string[];
  columnTypes: ValueType[];
}

export interface VectorCommonMetaData {
  numRecords: number;
  vectorContentType: number;
}

/**
 * Schema for a node type.
 * Properties are not stored here. they are stored in the row type, if any.
 */
export interface NodeType {
  /** use int32 for potential extensibility */
  nodeTypeId: number;
  nodeTypeName: Buffer;
  label: Buffer[];
}

/** for an edge type */
export interface EdgeType {
  edgeTypeId: number;
  edgeTypeName: Buffer;
  label: Buffer[];
}

export interface PropertyGraphSchema {
  graphId: number;
  graphName: Buffer;
  nodeType: NodeType[];
  edgeType: EdgeType[];
}

export interface VectorTableMetaData {
  tableType: number;
  numRecords: bigint;
  rowType?: RowType | undefined;
  numBatches: number;
  /** unit: minutes */
  timeZoneOffset: number;
  /** true: small endian, false: big endian */
  isLittleEndian: boolean;
  graphSchema: PropertyGraphSchema[];
}

export interface NestedVector {
  numNestedVectors: number;
  commonMetaData?: VectorCommonMetaData | undefined;
  specialMetaData: Buffer;
  vectorData: Buffer;
  nullBitMap: Buffer;
  nestedVectors: NestedVector[];
}

export interface VectorBatch {
  vectors: NestedVector[];
}

export interface VectorResultTable {
  dataLayoutVersion: Buffer;
  meta?: VectorTableMetaData | undefined;
  batch: VectorBatch[];
}

export interface ResultTable {
  columnNames: Buffer[];
  records: Row[];
}

function createBaseRow(): Row {
  return { values: [] };
}

export const Row: MessageFns<Row> = {
  encode(message: Row, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.values) {
      Value.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Row {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRow();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.values.push(Value.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Row {
    return { values: globalThis.Array.isArray(object?.values) ? object.values.map((e: any) => Value.fromJSON(e)) : [] };
  },

  toJSON(message: Row): unknown {
    const obj: any = {};
    if (message.values?.length) {
      obj.values = message.values.map((e) => Value.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Row>, I>>(base?: I): Row {
    return Row.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Row>, I>>(object: I): Row {
    const message = createBaseRow();
    message.values = object.values?.map((e) => Value.fromPartial(e)) || [];
    return message;
  },
};

function createBaseValueType(): ValueType {
  return { valueType: Buffer.alloc(0) };
}

export const ValueType: MessageFns<ValueType> = {
  encode(message: ValueType, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.valueType.length !== 0) {
      writer.uint32(10).bytes(message.valueType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ValueType {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseValueType();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.valueType = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ValueType {
    return { valueType: isSet(object.valueType) ? Buffer.from(bytesFromBase64(object.valueType)) : Buffer.alloc(0) };
  },

  toJSON(message: ValueType): unknown {
    const obj: any = {};
    if (message.valueType.length !== 0) {
      obj.valueType = base64FromBytes(message.valueType);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ValueType>, I>>(base?: I): ValueType {
    return ValueType.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ValueType>, I>>(object: I): ValueType {
    const message = createBaseValueType();
    message.valueType = object.valueType ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseRowType(): RowType {
  return { numColumns: 0, columnNames: [], columnTypes: [] };
}

export const RowType: MessageFns<RowType> = {
  encode(message: RowType, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.numColumns !== 0) {
      writer.uint32(8).uint32(message.numColumns);
    }
    for (const v of message.columnNames) {
      writer.uint32(18).string(v!);
    }
    for (const v of message.columnTypes) {
      ValueType.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RowType {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRowType();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.numColumns = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.columnNames.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.columnTypes.push(ValueType.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RowType {
    return {
      numColumns: isSet(object.numColumns) ? globalThis.Number(object.numColumns) : 0,
      columnNames: globalThis.Array.isArray(object?.columnNames)
        ? object.columnNames.map((e: any) => globalThis.String(e))
        : [],
      columnTypes: globalThis.Array.isArray(object?.columnTypes)
        ? object.columnTypes.map((e: any) => ValueType.fromJSON(e))
        : [],
    };
  },

  toJSON(message: RowType): unknown {
    const obj: any = {};
    if (message.numColumns !== 0) {
      obj.numColumns = Math.round(message.numColumns);
    }
    if (message.columnNames?.length) {
      obj.columnNames = message.columnNames;
    }
    if (message.columnTypes?.length) {
      obj.columnTypes = message.columnTypes.map((e) => ValueType.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RowType>, I>>(base?: I): RowType {
    return RowType.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RowType>, I>>(object: I): RowType {
    const message = createBaseRowType();
    message.numColumns = object.numColumns ?? 0;
    message.columnNames = object.columnNames?.map((e) => e) || [];
    message.columnTypes = object.columnTypes?.map((e) => ValueType.fromPartial(e)) || [];
    return message;
  },
};

function createBaseVectorCommonMetaData(): VectorCommonMetaData {
  return { numRecords: 0, vectorContentType: 0 };
}

export const VectorCommonMetaData: MessageFns<VectorCommonMetaData> = {
  encode(message: VectorCommonMetaData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.numRecords !== 0) {
      writer.uint32(8).uint32(message.numRecords);
    }
    if (message.vectorContentType !== 0) {
      writer.uint32(16).uint32(message.vectorContentType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VectorCommonMetaData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVectorCommonMetaData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.numRecords = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.vectorContentType = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VectorCommonMetaData {
    return {
      numRecords: isSet(object.numRecords) ? globalThis.Number(object.numRecords) : 0,
      vectorContentType: isSet(object.vectorContentType) ? globalThis.Number(object.vectorContentType) : 0,
    };
  },

  toJSON(message: VectorCommonMetaData): unknown {
    const obj: any = {};
    if (message.numRecords !== 0) {
      obj.numRecords = Math.round(message.numRecords);
    }
    if (message.vectorContentType !== 0) {
      obj.vectorContentType = Math.round(message.vectorContentType);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VectorCommonMetaData>, I>>(base?: I): VectorCommonMetaData {
    return VectorCommonMetaData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VectorCommonMetaData>, I>>(object: I): VectorCommonMetaData {
    const message = createBaseVectorCommonMetaData();
    message.numRecords = object.numRecords ?? 0;
    message.vectorContentType = object.vectorContentType ?? 0;
    return message;
  },
};

function createBaseNodeType(): NodeType {
  return { nodeTypeId: 0, nodeTypeName: Buffer.alloc(0), label: [] };
}

export const NodeType: MessageFns<NodeType> = {
  encode(message: NodeType, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nodeTypeId !== 0) {
      writer.uint32(8).int32(message.nodeTypeId);
    }
    if (message.nodeTypeName.length !== 0) {
      writer.uint32(18).bytes(message.nodeTypeName);
    }
    for (const v of message.label) {
      writer.uint32(26).bytes(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NodeType {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNodeType();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.nodeTypeId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nodeTypeName = Buffer.from(reader.bytes());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.label.push(Buffer.from(reader.bytes()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NodeType {
    return {
      nodeTypeId: isSet(object.nodeTypeId) ? globalThis.Number(object.nodeTypeId) : 0,
      nodeTypeName: isSet(object.nodeTypeName) ? Buffer.from(bytesFromBase64(object.nodeTypeName)) : Buffer.alloc(0),
      label: globalThis.Array.isArray(object?.label)
        ? object.label.map((e: any) => Buffer.from(bytesFromBase64(e)))
        : [],
    };
  },

  toJSON(message: NodeType): unknown {
    const obj: any = {};
    if (message.nodeTypeId !== 0) {
      obj.nodeTypeId = Math.round(message.nodeTypeId);
    }
    if (message.nodeTypeName.length !== 0) {
      obj.nodeTypeName = base64FromBytes(message.nodeTypeName);
    }
    if (message.label?.length) {
      obj.label = message.label.map((e) => base64FromBytes(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NodeType>, I>>(base?: I): NodeType {
    return NodeType.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NodeType>, I>>(object: I): NodeType {
    const message = createBaseNodeType();
    message.nodeTypeId = object.nodeTypeId ?? 0;
    message.nodeTypeName = object.nodeTypeName ?? Buffer.alloc(0);
    message.label = object.label?.map((e) => e) || [];
    return message;
  },
};

function createBaseEdgeType(): EdgeType {
  return { edgeTypeId: 0, edgeTypeName: Buffer.alloc(0), label: [] };
}

export const EdgeType: MessageFns<EdgeType> = {
  encode(message: EdgeType, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.edgeTypeId !== 0) {
      writer.uint32(8).int32(message.edgeTypeId);
    }
    if (message.edgeTypeName.length !== 0) {
      writer.uint32(18).bytes(message.edgeTypeName);
    }
    for (const v of message.label) {
      writer.uint32(26).bytes(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EdgeType {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEdgeType();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.edgeTypeId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.edgeTypeName = Buffer.from(reader.bytes());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.label.push(Buffer.from(reader.bytes()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EdgeType {
    return {
      edgeTypeId: isSet(object.edgeTypeId) ? globalThis.Number(object.edgeTypeId) : 0,
      edgeTypeName: isSet(object.edgeTypeName) ? Buffer.from(bytesFromBase64(object.edgeTypeName)) : Buffer.alloc(0),
      label: globalThis.Array.isArray(object?.label)
        ? object.label.map((e: any) => Buffer.from(bytesFromBase64(e)))
        : [],
    };
  },

  toJSON(message: EdgeType): unknown {
    const obj: any = {};
    if (message.edgeTypeId !== 0) {
      obj.edgeTypeId = Math.round(message.edgeTypeId);
    }
    if (message.edgeTypeName.length !== 0) {
      obj.edgeTypeName = base64FromBytes(message.edgeTypeName);
    }
    if (message.label?.length) {
      obj.label = message.label.map((e) => base64FromBytes(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EdgeType>, I>>(base?: I): EdgeType {
    return EdgeType.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EdgeType>, I>>(object: I): EdgeType {
    const message = createBaseEdgeType();
    message.edgeTypeId = object.edgeTypeId ?? 0;
    message.edgeTypeName = object.edgeTypeName ?? Buffer.alloc(0);
    message.label = object.label?.map((e) => e) || [];
    return message;
  },
};

function createBasePropertyGraphSchema(): PropertyGraphSchema {
  return { graphId: 0, graphName: Buffer.alloc(0), nodeType: [], edgeType: [] };
}

export const PropertyGraphSchema: MessageFns<PropertyGraphSchema> = {
  encode(message: PropertyGraphSchema, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.graphId !== 0) {
      writer.uint32(8).int32(message.graphId);
    }
    if (message.graphName.length !== 0) {
      writer.uint32(18).bytes(message.graphName);
    }
    for (const v of message.nodeType) {
      NodeType.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.edgeType) {
      EdgeType.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PropertyGraphSchema {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePropertyGraphSchema();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.graphId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.graphName = Buffer.from(reader.bytes());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.nodeType.push(NodeType.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.edgeType.push(EdgeType.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PropertyGraphSchema {
    return {
      graphId: isSet(object.graphId) ? globalThis.Number(object.graphId) : 0,
      graphName: isSet(object.graphName) ? Buffer.from(bytesFromBase64(object.graphName)) : Buffer.alloc(0),
      nodeType: globalThis.Array.isArray(object?.nodeType) ? object.nodeType.map((e: any) => NodeType.fromJSON(e)) : [],
      edgeType: globalThis.Array.isArray(object?.edgeType) ? object.edgeType.map((e: any) => EdgeType.fromJSON(e)) : [],
    };
  },

  toJSON(message: PropertyGraphSchema): unknown {
    const obj: any = {};
    if (message.graphId !== 0) {
      obj.graphId = Math.round(message.graphId);
    }
    if (message.graphName.length !== 0) {
      obj.graphName = base64FromBytes(message.graphName);
    }
    if (message.nodeType?.length) {
      obj.nodeType = message.nodeType.map((e) => NodeType.toJSON(e));
    }
    if (message.edgeType?.length) {
      obj.edgeType = message.edgeType.map((e) => EdgeType.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PropertyGraphSchema>, I>>(base?: I): PropertyGraphSchema {
    return PropertyGraphSchema.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PropertyGraphSchema>, I>>(object: I): PropertyGraphSchema {
    const message = createBasePropertyGraphSchema();
    message.graphId = object.graphId ?? 0;
    message.graphName = object.graphName ?? Buffer.alloc(0);
    message.nodeType = object.nodeType?.map((e) => NodeType.fromPartial(e)) || [];
    message.edgeType = object.edgeType?.map((e) => EdgeType.fromPartial(e)) || [];
    return message;
  },
};

function createBaseVectorTableMetaData(): VectorTableMetaData {
  return {
    tableType: 0,
    numRecords: 0n,
    rowType: undefined,
    numBatches: 0,
    timeZoneOffset: 0,
    isLittleEndian: false,
    graphSchema: [],
  };
}

export const VectorTableMetaData: MessageFns<VectorTableMetaData> = {
  encode(message: VectorTableMetaData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tableType !== 0) {
      writer.uint32(8).uint32(message.tableType);
    }
    if (message.numRecords !== 0n) {
      if (BigInt.asUintN(64, message.numRecords) !== message.numRecords) {
        throw new globalThis.Error("value provided for field message.numRecords of type uint64 too large");
      }
      writer.uint32(16).uint64(message.numRecords);
    }
    if (message.rowType !== undefined) {
      RowType.encode(message.rowType, writer.uint32(26).fork()).join();
    }
    if (message.numBatches !== 0) {
      writer.uint32(32).uint32(message.numBatches);
    }
    if (message.timeZoneOffset !== 0) {
      writer.uint32(40).uint32(message.timeZoneOffset);
    }
    if (message.isLittleEndian !== false) {
      writer.uint32(48).bool(message.isLittleEndian);
    }
    for (const v of message.graphSchema) {
      PropertyGraphSchema.encode(v!, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VectorTableMetaData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVectorTableMetaData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.tableType = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.numRecords = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.rowType = RowType.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.numBatches = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.timeZoneOffset = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.isLittleEndian = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.graphSchema.push(PropertyGraphSchema.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VectorTableMetaData {
    return {
      tableType: isSet(object.tableType) ? globalThis.Number(object.tableType) : 0,
      numRecords: isSet(object.numRecords) ? BigInt(object.numRecords) : 0n,
      rowType: isSet(object.rowType) ? RowType.fromJSON(object.rowType) : undefined,
      numBatches: isSet(object.numBatches) ? globalThis.Number(object.numBatches) : 0,
      timeZoneOffset: isSet(object.timeZoneOffset) ? globalThis.Number(object.timeZoneOffset) : 0,
      isLittleEndian: isSet(object.isLittleEndian) ? globalThis.Boolean(object.isLittleEndian) : false,
      graphSchema: globalThis.Array.isArray(object?.graphSchema)
        ? object.graphSchema.map((e: any) => PropertyGraphSchema.fromJSON(e))
        : [],
    };
  },

  toJSON(message: VectorTableMetaData): unknown {
    const obj: any = {};
    if (message.tableType !== 0) {
      obj.tableType = Math.round(message.tableType);
    }
    if (message.numRecords !== 0n) {
      obj.numRecords = message.numRecords.toString();
    }
    if (message.rowType !== undefined) {
      obj.rowType = RowType.toJSON(message.rowType);
    }
    if (message.numBatches !== 0) {
      obj.numBatches = Math.round(message.numBatches);
    }
    if (message.timeZoneOffset !== 0) {
      obj.timeZoneOffset = Math.round(message.timeZoneOffset);
    }
    if (message.isLittleEndian !== false) {
      obj.isLittleEndian = message.isLittleEndian;
    }
    if (message.graphSchema?.length) {
      obj.graphSchema = message.graphSchema.map((e) => PropertyGraphSchema.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VectorTableMetaData>, I>>(base?: I): VectorTableMetaData {
    return VectorTableMetaData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VectorTableMetaData>, I>>(object: I): VectorTableMetaData {
    const message = createBaseVectorTableMetaData();
    message.tableType = object.tableType ?? 0;
    message.numRecords = object.numRecords ?? 0n;
    message.rowType = (object.rowType !== undefined && object.rowType !== null)
      ? RowType.fromPartial(object.rowType)
      : undefined;
    message.numBatches = object.numBatches ?? 0;
    message.timeZoneOffset = object.timeZoneOffset ?? 0;
    message.isLittleEndian = object.isLittleEndian ?? false;
    message.graphSchema = object.graphSchema?.map((e) => PropertyGraphSchema.fromPartial(e)) || [];
    return message;
  },
};

function createBaseNestedVector(): NestedVector {
  return {
    numNestedVectors: 0,
    commonMetaData: undefined,
    specialMetaData: Buffer.alloc(0),
    vectorData: Buffer.alloc(0),
    nullBitMap: Buffer.alloc(0),
    nestedVectors: [],
  };
}

export const NestedVector: MessageFns<NestedVector> = {
  encode(message: NestedVector, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.numNestedVectors !== 0) {
      writer.uint32(8).uint32(message.numNestedVectors);
    }
    if (message.commonMetaData !== undefined) {
      VectorCommonMetaData.encode(message.commonMetaData, writer.uint32(18).fork()).join();
    }
    if (message.specialMetaData.length !== 0) {
      writer.uint32(26).bytes(message.specialMetaData);
    }
    if (message.vectorData.length !== 0) {
      writer.uint32(34).bytes(message.vectorData);
    }
    if (message.nullBitMap.length !== 0) {
      writer.uint32(42).bytes(message.nullBitMap);
    }
    for (const v of message.nestedVectors) {
      NestedVector.encode(v!, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NestedVector {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNestedVector();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.numNestedVectors = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.commonMetaData = VectorCommonMetaData.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.specialMetaData = Buffer.from(reader.bytes());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.vectorData = Buffer.from(reader.bytes());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.nullBitMap = Buffer.from(reader.bytes());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.nestedVectors.push(NestedVector.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NestedVector {
    return {
      numNestedVectors: isSet(object.numNestedVectors) ? globalThis.Number(object.numNestedVectors) : 0,
      commonMetaData: isSet(object.commonMetaData) ? VectorCommonMetaData.fromJSON(object.commonMetaData) : undefined,
      specialMetaData: isSet(object.specialMetaData)
        ? Buffer.from(bytesFromBase64(object.specialMetaData))
        : Buffer.alloc(0),
      vectorData: isSet(object.vectorData) ? Buffer.from(bytesFromBase64(object.vectorData)) : Buffer.alloc(0),
      nullBitMap: isSet(object.nullBitMap) ? Buffer.from(bytesFromBase64(object.nullBitMap)) : Buffer.alloc(0),
      nestedVectors: globalThis.Array.isArray(object?.nestedVectors)
        ? object.nestedVectors.map((e: any) => NestedVector.fromJSON(e))
        : [],
    };
  },

  toJSON(message: NestedVector): unknown {
    const obj: any = {};
    if (message.numNestedVectors !== 0) {
      obj.numNestedVectors = Math.round(message.numNestedVectors);
    }
    if (message.commonMetaData !== undefined) {
      obj.commonMetaData = VectorCommonMetaData.toJSON(message.commonMetaData);
    }
    if (message.specialMetaData.length !== 0) {
      obj.specialMetaData = base64FromBytes(message.specialMetaData);
    }
    if (message.vectorData.length !== 0) {
      obj.vectorData = base64FromBytes(message.vectorData);
    }
    if (message.nullBitMap.length !== 0) {
      obj.nullBitMap = base64FromBytes(message.nullBitMap);
    }
    if (message.nestedVectors?.length) {
      obj.nestedVectors = message.nestedVectors.map((e) => NestedVector.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NestedVector>, I>>(base?: I): NestedVector {
    return NestedVector.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NestedVector>, I>>(object: I): NestedVector {
    const message = createBaseNestedVector();
    message.numNestedVectors = object.numNestedVectors ?? 0;
    message.commonMetaData = (object.commonMetaData !== undefined && object.commonMetaData !== null)
      ? VectorCommonMetaData.fromPartial(object.commonMetaData)
      : undefined;
    message.specialMetaData = object.specialMetaData ?? Buffer.alloc(0);
    message.vectorData = object.vectorData ?? Buffer.alloc(0);
    message.nullBitMap = object.nullBitMap ?? Buffer.alloc(0);
    message.nestedVectors = object.nestedVectors?.map((e) => NestedVector.fromPartial(e)) || [];
    return message;
  },
};

function createBaseVectorBatch(): VectorBatch {
  return { vectors: [] };
}

export const VectorBatch: MessageFns<VectorBatch> = {
  encode(message: VectorBatch, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.vectors) {
      NestedVector.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VectorBatch {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVectorBatch();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.vectors.push(NestedVector.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VectorBatch {
    return {
      vectors: globalThis.Array.isArray(object?.vectors)
        ? object.vectors.map((e: any) => NestedVector.fromJSON(e))
        : [],
    };
  },

  toJSON(message: VectorBatch): unknown {
    const obj: any = {};
    if (message.vectors?.length) {
      obj.vectors = message.vectors.map((e) => NestedVector.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VectorBatch>, I>>(base?: I): VectorBatch {
    return VectorBatch.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VectorBatch>, I>>(object: I): VectorBatch {
    const message = createBaseVectorBatch();
    message.vectors = object.vectors?.map((e) => NestedVector.fromPartial(e)) || [];
    return message;
  },
};

function createBaseVectorResultTable(): VectorResultTable {
  return { dataLayoutVersion: Buffer.alloc(0), meta: undefined, batch: [] };
}

export const VectorResultTable: MessageFns<VectorResultTable> = {
  encode(message: VectorResultTable, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dataLayoutVersion.length !== 0) {
      writer.uint32(10).bytes(message.dataLayoutVersion);
    }
    if (message.meta !== undefined) {
      VectorTableMetaData.encode(message.meta, writer.uint32(18).fork()).join();
    }
    for (const v of message.batch) {
      VectorBatch.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VectorResultTable {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVectorResultTable();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dataLayoutVersion = Buffer.from(reader.bytes());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.meta = VectorTableMetaData.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.batch.push(VectorBatch.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VectorResultTable {
    return {
      dataLayoutVersion: isSet(object.dataLayoutVersion)
        ? Buffer.from(bytesFromBase64(object.dataLayoutVersion))
        : Buffer.alloc(0),
      meta: isSet(object.meta) ? VectorTableMetaData.fromJSON(object.meta) : undefined,
      batch: globalThis.Array.isArray(object?.batch) ? object.batch.map((e: any) => VectorBatch.fromJSON(e)) : [],
    };
  },

  toJSON(message: VectorResultTable): unknown {
    const obj: any = {};
    if (message.dataLayoutVersion.length !== 0) {
      obj.dataLayoutVersion = base64FromBytes(message.dataLayoutVersion);
    }
    if (message.meta !== undefined) {
      obj.meta = VectorTableMetaData.toJSON(message.meta);
    }
    if (message.batch?.length) {
      obj.batch = message.batch.map((e) => VectorBatch.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VectorResultTable>, I>>(base?: I): VectorResultTable {
    return VectorResultTable.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VectorResultTable>, I>>(object: I): VectorResultTable {
    const message = createBaseVectorResultTable();
    message.dataLayoutVersion = object.dataLayoutVersion ?? Buffer.alloc(0);
    message.meta = (object.meta !== undefined && object.meta !== null)
      ? VectorTableMetaData.fromPartial(object.meta)
      : undefined;
    message.batch = object.batch?.map((e) => VectorBatch.fromPartial(e)) || [];
    return message;
  },
};

function createBaseResultTable(): ResultTable {
  return { columnNames: [], records: [] };
}

export const ResultTable: MessageFns<ResultTable> = {
  encode(message: ResultTable, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.columnNames) {
      writer.uint32(10).bytes(v!);
    }
    for (const v of message.records) {
      Row.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResultTable {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResultTable();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.columnNames.push(Buffer.from(reader.bytes()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.records.push(Row.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResultTable {
    return {
      columnNames: globalThis.Array.isArray(object?.columnNames)
        ? object.columnNames.map((e: any) => Buffer.from(bytesFromBase64(e)))
        : [],
      records: globalThis.Array.isArray(object?.records) ? object.records.map((e: any) => Row.fromJSON(e)) : [],
    };
  },

  toJSON(message: ResultTable): unknown {
    const obj: any = {};
    if (message.columnNames?.length) {
      obj.columnNames = message.columnNames.map((e) => base64FromBytes(e));
    }
    if (message.records?.length) {
      obj.records = message.records.map((e) => Row.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResultTable>, I>>(base?: I): ResultTable {
    return ResultTable.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResultTable>, I>>(object: I): ResultTable {
    const message = createBaseResultTable();
    message.columnNames = object.columnNames?.map((e) => e) || [];
    message.records = object.records?.map((e) => Row.fromPartial(e)) || [];
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
